module Kantour.Core.KcData.Master.CompletenessSpec
  ( spec
  )
where

import Test.Hspec

{-
  This module verifies that all fields in source master data
  are being recognized properly.
 -}

spec :: Spec
spec = describe "demo" $ specify "trivial" $ () `shouldBe` ()
